[package]
name = "resharp-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[lib]
crate-type = ["cdylib"]
test = false

[features]
experimental_capture_groups = ["resharp/experimental_capture_groups"]

[dependencies]
resharp.workspace = true
